# Meeting Engine

One-page marketing funnel for Meeting Engine — performance-based cold email that books qualified sales calls. Clients pay per qualified call that shows, plus a ~$600/month system fee.

This is a static site (Vite). Host the `dist/` folder on any domain.

## Local preview

